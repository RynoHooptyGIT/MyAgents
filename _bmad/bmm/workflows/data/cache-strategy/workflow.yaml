name: cache-strategy
description: "Redis cache strategy (invalidation, TTL, data structures, cache-aside vs write-through)"
author: "BMAD"
config_source: "{project-root}/_bmad/bmm/config.yaml"
output_folder: "{config_source}:output_folder"
user_name: "{config_source}:user_name"
communication_language: "{config_source}:communication_language"
installed_path: "{project-root}/_bmad/bmm/workflows/data/cache-strategy"
instructions: "{installed_path}/instructions.md"
variables:
  agent: "oracle"
input_file_patterns:
  project_context:
    description: "Project context rules file"
    whole: "**/project-context.md"
    load_strategy: "FULL_LOAD"
  models:
    description: "SQLAlchemy models directory"
    whole: "backend/app/models/"
    load_strategy: "DIRECTORY_LISTING"
  migrations:
    description: "Alembic migrations directory"
    whole: "backend/alembic/versions/"
    load_strategy: "DIRECTORY_LISTING"
standalone: true
