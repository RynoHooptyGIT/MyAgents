---
description: 'Execute a story through the canonical Phase-4 loop — clarify, plan, implement, review, present — with sprint-status sync'
---

IT IS CRITICAL THAT YOU FOLLOW THIS COMMAND: LOAD the FULL @team/workflows/implementation/quick-dev/workflow.md, READ its entire contents and follow its directions exactly!

> Replaces the deprecated `dev-story` workflow (upstream bmad #2641). The command
> name is retained as the familiar entry point; the workflow it runs is
> `team/workflows/implementation/quick-dev/`, the canonical Phase-4 loop ported
> from bmad v6.10.0.
>
> Not to be confused with `/team:quick-dev`, which runs the fork's separate
> lightweight `team/workflows/quick-flow/quick-dev/` workflow.
