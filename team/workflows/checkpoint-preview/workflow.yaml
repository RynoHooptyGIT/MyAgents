name: checkpoint-preview
description: "LLM-assisted human-in-the-loop review — walks through a change from purpose to details"
author: "Maestro"

# Critical variables from config
config_source: "{project-root}/team/config.yaml"
user_name: "{config_source}:user_name"
communication_language: "{config_source}:communication_language"
date: system-generated
planning_artifacts: "{config_source}:planning_artifacts"
implementation_artifacts: "{config_source}:implementation_artifacts"
output_folder: "{config_source}:output_folder"

# Workflow components
installed_path: "{project-root}/team/workflows/checkpoint-preview"
template: false
instructions: "{installed_path}/instructions.xml"

# Variables
variables:
  sprint_status: "{implementation_artifacts}/sprint-status.yaml"
  project_context: "{output_folder}/project-context.md"

standalone: true
