name: branch-cleanup
description: "Clean up local git branches deleted on remote ([gone] branches) and their worktrees"
author: "BMAD"
config_source: "{project-root}/team/config.yaml"
output_folder: "{config_source}:output_folder"
user_name: "{config_source}:user_name"
communication_language: "{config_source}:communication_language"
installed_path: "{project-root}/team/workflows/devops/branch-cleanup"
instructions: "{installed_path}/instructions.md"
variables:
  agent: "forge"
standalone: true
