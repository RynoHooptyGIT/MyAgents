# Shared install manifest — the single list of tooling files that scripts/setup.sh
# (fresh install) and scripts/team-update.sh (update from upstream) both install.
# Applied by apply_manifest in scripts/lib/install-manifest.sh.
#
# Format: mode<TAB>src<TAB>dst   (# comments and blank lines are ignored)
#   copy  copy, overwriting dst
#   init  copy only if dst is absent (project-owned after the first install)
#   exec  copy, overwriting dst, then chmod +x
# src is relative to the source root; a glob is allowed in the basename only.
# dst is relative to the destination root; a trailing / means "into this directory",
# otherwise it is the destination file path (single-file src only).

# Claude Code hooks
exec	.claude/hooks/check-for-updates.sh	.claude/hooks/
exec	.claude/hooks/slim-prompt.sh	.claude/hooks/
exec	hooks/post-commit-context.sh	.claude/hooks/

# Coordination hooks
exec	.agents/hooks/worktree-guard.sh	.agents/hooks/
exec	.agents/hooks/claim-check.sh	.agents/hooks/
exec	.agents/hooks/heartbeat.sh	.agents/hooks/
copy	.agents/hooks/lib-identity.sh	.agents/hooks/

# Instinct capture loop hooks
exec	.agents/hooks/observe.sh	.agents/hooks/
exec	.agents/hooks/instinct-mine.sh	.agents/hooks/
exec	.agents/hooks/instinct-inject.sh	.agents/hooks/

# Python tooling (instinct CLI, shared config loader, context generators)
copy	scripts/instincts/*.py	scripts/instincts/
copy	scripts/lib/*.py	scripts/lib/
copy	scripts/lib/install-manifest.sh	scripts/lib/
copy	scripts/context/*.py	scripts/context/
copy	scripts/context/context-config.example.yaml	scripts/context/
init	scripts/context/context-config.yaml	scripts/context/

# Shell tooling
exec	scripts/team-update.sh	scripts/
exec	scripts/team-check.sh	scripts/
exec	scripts/test.sh	scripts/
exec	scripts/check-settings-drift.sh	scripts/

# Project-owned after the first install (never clobbered by an update)
init	.agents/config.yaml	.agents/
init	templates/settings.local.json.template	.claude/settings.local.json
