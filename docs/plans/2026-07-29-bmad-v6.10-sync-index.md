# bmad v6.10.0 Sync — Port Plan Index

> **STATUS: COMPLETE** (2026-08-23). All eight plans resolved: A–F landed, G verified no-op, H landed with divergences.
>
> **Baseline correction:** this index states "v6.6.0 (last sync) → v6.10.0, 132 commits". That was wrong — the trunk (`origin/main`) was already synced to **v6.9.0**. The true delta was **24 commits** (`d3abb6da` → `081e64ee`). Plans were re-baselined before execution.
>
> **Deviations from this index:** `create-story` and `bmad-investigate` are deliberately retained; the final `--mark-synced` step was NOT run as written (it would have recorded 6.11.0). See `docs/sync-notes-v6.10.0.md`.

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement each subsystem plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Port relevant bmad upstream changes (v6.6.0 → v6.10.0) into the fork across 8 subsystem plans.

**Architecture:** Each subsystem is an independently shippable port with its own plan file. Fork layout (`team/agents/*.md`, `team/workflows/<phase>/<workflow>/`) is preserved. Bmad's `src/bmm-skills/<phase>/<skill>/{SKILL.md, customize.toml, references/}` maps to `team/workflows/<phase>/<workflow>/{skill.md, customize.toml, references/}` per the pattern established by the investigate port (commit `f2014fd9`).

**Upstream span:** v6.6.0 (last sync, ref `b5b33c08`) → v6.10.0 (`bmad/main`), 132 commits, 5 tagged releases.

**Excluded from all plans:**
- `website/` — fork does not publish a website
- `tools/installer`, `tools/cli` — fork uses `scripts/team-update.sh` instead
- `docs/zh-cn/` — fork is English-only
- `web-bundles/` (v6.9.0 web bundle system) — fork does not distribute web bundles
- Anything under bmad's `src/modules/` that is installer-only

---

## Subsystem plans

Ordered by dependency (top → bottom). Each row is a plan file.

| # | Plan | Upstream refs | Depends on | Risk |
|---|---|---|---|---|
| A | [2026-07-29-bmad-v6.10-phase4-impl-loop.md](2026-07-29-bmad-v6.10-phase4-impl-loop.md) | #2637, #2549, #2635, #2629, #2561, #2643 | — | High (replaces core dev loop) |
| B | [2026-07-29-bmad-v6.10-core-8-skill-consolidation.md](2026-07-29-bmad-v6.10-core-8-skill-consolidation.md) | #2603, #2608, #2611 | A | High (restructures skill tree) |
| C | [2026-07-29-bmad-v6.10-review-layers.md](2026-07-29-bmad-v6.10-review-layers.md) | #2550, #2535, #2647, #2646, #2642 | A, B | Medium |
| D | [2026-07-29-bmad-v6.10-new-skills.md](2026-07-29-bmad-v6.10-new-skills.md) | #2492 (forge-idea), #2417 (spec), #2378, #2385 (prd), #2413 (ux) | B | Medium (additive) |
| E | [2026-07-29-bmad-v6.10-party-mode.md](2026-07-29-bmad-v6.10-party-mode.md) | #2530, #2484, #2539, #2531 | — | Low |
| F | [2026-07-29-bmad-v6.10-elicitation-expansion.md](2026-07-29-bmad-v6.10-elicitation-expansion.md) | #2062 (+19 methods), #2515 | — | Low (data-only) |
| G | [2026-07-29-bmad-v6.10-edge-case-hunter.md](2026-07-29-bmad-v6.10-edge-case-hunter.md) | #2524, #2525 | C | Low |
| H | [2026-07-29-bmad-v6.10-deprecations.md](2026-07-29-bmad-v6.10-deprecations.md) | #2641 (create-story, dev-story), #2532 (automator) | A, B, D | Low |

**Suggested execution order:** F → E → A → B → D → C → G → H
(Low-risk data and party-mode ports first to build confidence; A → B before D so new skills slot into consolidated tree; C after A/B because reviewers reference the new impl loop; G after C; H last since it deletes what earlier plans replace.)

---

## Verification (applies to every plan)

Each plan must, before marking complete:

1. **Static check:** any new `skill.md` renders under fork's skill loader — verify with `grep -rn '^---$' team/workflows/<path>/skill.md` returns exactly two matches (frontmatter delimiters).
2. **Manifest check:** if a new agent or workflow is added, `team/agent-manifest.csv` or `team/manifest.yaml` is updated. Run `bash scripts/team-check.sh` and confirm no new errors.
3. **Reference integrity:** any `references/` file linked from `skill.md` exists — verify with `grep -oE 'references/[a-z0-9-]+\.md' team/workflows/<path>/skill.md | while read r; do [ -f "team/workflows/<path>/$r" ] || echo MISSING $r; done`.
4. **Commit hygiene:** commits reference the bmad upstream hash(es) they port from, matching the pattern from `f2014fd9` (`Ported from bmad commits: <hash1>, <hash2>`).

---

## Final step (across all plans)

- [ ] After all 8 plans land, mark the sync complete:

```bash
bash scripts/sync-upstream.sh --mark-synced
```

Expected: writes `6.10.0` to `.team-upstream-version` and current `bmad/main` HEAD to `.team-last-sync-tag`.

- [ ] Verify the update-available marker cleared:

```bash
[ -f .team-update-available ] && echo "STILL PRESENT" || echo "CLEARED"
```

Expected: `CLEARED`.

- [ ] Commit the sync marker:

```bash
git add .team-upstream-version .team-last-sync-tag
git commit -m "chore: mark synced with bmad upstream v6.10.0"
```
