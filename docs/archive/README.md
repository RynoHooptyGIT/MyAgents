# Archive

Completed plan documents move here once their work has landed and the plan
is no longer an active reference for implementers. Files in this directory
are frozen: they are historical record and are not edited except to correct
a broken cross-reference into them. Design specs stay in `docs/specs/` even
after the plans that implemented them are archived — specs remain the
design record, not the plans.
